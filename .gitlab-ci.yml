stages:
  - install
  - build
  - publish

include:
  - project: devops/pipes
    file: /templates/node.gitlab-ci.yml
    ref: main

  - project: devops/pipes
    file: /templates/semantic-release.gitlab-ci.yml
    ref: main

build:
  artifacts:
    paths:
      - dist/
      - oclif.manifest.json
