export const OUTPUT_FILE_ACCEPTED_TYPES = ['.pdf']

export const INPUT_FILE_ACCEPTED_TYPES = ['.md', '.yml', '.yaml']

export enum OutputFileType {
  PDF = 'pdf',
  HTML = 'html'
}

export enum InputFileType {
  MARKDOWN = 'md',
  YAML = 'yaml',
  YAML_SHORT = 'yml'
}
